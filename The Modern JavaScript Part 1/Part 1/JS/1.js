// Working with Variables

let admin, name;
name = 'John';
admin = name;

console.log(admin);

// Alert Method : It has no return Value

alert('Hi Mr. Billionare');

// Prompt Method : Returns a text , or null if ESC or cancel is clicked

let userAge = prompt('What is your age Brother ?');
console.log(userAge);

// Confirm Method : Returns a Boolean Value True/False

let isMarried = confirm('Are you Married ?');
console.log(isMarried);

// WAP to ask a name and then Alert it 

let userCurrentName = prompt('What is your Name ?');
alert(userCurrentName);
